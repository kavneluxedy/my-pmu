# Base SQLite locale (fichier). Copiez ce fichier en .env avant la 1re migration.
DATABASE_URL="file:./pmu.db"
PORT=3001
# Autorise le frontend Vite en développement.
CORS_ORIGIN="http://localhost:5173"
